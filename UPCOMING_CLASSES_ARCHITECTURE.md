# Upcoming Classes - Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           LEARNING DASHBOARD (UI)                           │
│                     components/dashboard/learning-dashboard                  │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │
                                   │ useEffect + Hooks
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        ZUSTAND STORE (State)                                │
│                  lib/branch-system/stores/branch-classes.store.ts           │
│                                                                             │
│  State:                                                                     │
│    • upcomingClassesCache: Record<studentId, {data, timestamp}>            │
│    • loading.upcomingClasses: boolean                                      │
│    • errors.upcomingClasses: string | null                                 │
│    • inflightRequests: Record<key, Promise>                                │
│                                                                             │
│  Actions:                                                                   │
│    • fetchUpcomingClasses(studentId, forceRefresh?)                        │
│    • getUpcomingClasses(studentId)                                         │
│                                                                             │
│  Hooks:                                                                     │
│    • useUpcomingClasses(studentId)                                         │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
              Check Cache?                   Call Service
                    │                             │
            ┌───────┴────────┐                   ▼
            │                │          ┌─────────────────────┐
       Cache Valid?     Cache Miss      │   SERVICE LAYER     │
            │                │          │ branch-classes      │
            ▼                │          │    .service.ts      │
       Return Data           │          └──────────┬──────────┘
                             │                     │
                             │              Call RPC Function
                             │                     │
                             │                     ▼
                             │          ┌─────────────────────┐
                             │          │   SUPABASE RPC      │
                             │          │ get_upcoming_       │
                             │          │    classes          │
                             │          └──────────┬──────────┘
                             │                     │
                             │              Database Query
                             │                     │
                             │                     ▼
                             │          ┌─────────────────────┐
                             │          │    DATABASE         │
                             │          │  • class_enrollments│
                             │          │  • branch_classes   │
                             │          └──────────┬──────────┘
                             │                     │
                             │              Return Results
                             │                     │
                             └─────────────────────┘
                                                   │
                                    Cache + Return to Store
                                                   │
                                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          UTILS (Transform)                                  │
│                   lib/branch-system/utils/branch-classes.utils.ts           │
│                                                                             │
│  Functions:                                                                 │
│    • mapUpcomingClassData(rpcData) → UIData                                │
│    • mapSubjectToId(subject) → subjectId                                   │
│    • getSubjectColor(subjectId) → colorClass                               │
│    • formatTime(time) → "02:30 PM"                                         │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │
                            Transform Data
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         UI COMPONENTS                                       │
│                                                                             │
│    ┌─────────────────────┐         ┌─────────────────────┐               │
│    │  UpcomingClasses    │         │  UpcomingClassCard  │               │
│    │  Container          │────────▶│  Individual Card     │               │
│    └─────────────────────┘         └─────────────────────┘               │
│                                                                             │
│    Displays:                                                                │
│      • Class name & subject                                                │
│      • Start time                                                          │
│      • Subject badge with color                                            │
│      • Participant avatars                                                 │
│      • "Start" button                                                      │
└─────────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                           DATA FLOW SEQUENCE                                │
└─────────────────────────────────────────────────────────────────────────────┘

1. Component Mount
   └─► useEffect triggers

2. Call fetchUpcomingClasses(studentId)
   └─► Store checks cache
       ├─► Cache Hit (< 5 min) → Return cached data
       └─► Cache Miss → Continue to step 3

3. Check Inflight Requests
   ├─► Already fetching → Wait for existing promise
   └─► Not fetching → Continue to step 4

4. Set Loading State
   └─► loading.upcomingClasses = true

5. Call Service Layer
   └─► branchClassesService.getUpcomingClasses(studentId)

6. Execute RPC
   └─► supabase.rpc('get_upcoming_classes', { p_student_id })

7. Database Query
   └─► JOIN class_enrollments + branch_classes
       └─► WHERE enrolled + active + not ended

8. Return Results
   └─► Service receives UpcomingClassData[]

9. Cache Results
   └─► Store saves with timestamp
       └─► upcomingClassesCache[studentId] = { data, timestamp }

10. Component Receives Data
    └─► useUpcomingClasses hook returns cached data

11. Transform Data
    └─► map(mapUpcomingClassData) → UpcomingClass[]

12. Render UI
    └─► UpcomingClasses → UpcomingClassCard


┌─────────────────────────────────────────────────────────────────────────────┐
│                         CACHE STRATEGY                                      │
└─────────────────────────────────────────────────────────────────────────────┘

Cache Structure:
┌─────────────────────────────────────┐
│ upcomingClassesCache                │
│  ├─ [studentId_1]                   │
│  │   ├─ data: UpcomingClassData[]   │
│  │   └─ timestamp: number           │
│  ├─ [studentId_2]                   │
│  │   ├─ data: UpcomingClassData[]   │
│  │   └─ timestamp: number           │
│  └─ ...                              │
└─────────────────────────────────────┘

Cache Logic:
┌───────────────┐
│  Fetch Call   │
└───────┬───────┘
        │
        ▼
   ┌─────────────┐
   │Check Cache? │
   └──────┬──────┘
          │
    ┌─────┴─────┐
    │           │
  Exists?    Missing
    │           │
    ▼           ▼
┌─────────┐  ┌─────────┐
│Check Age│  │  Fetch  │
└────┬────┘  │  New    │
     │       └────┬────┘
┌────┴────┐       │
│< 5 min? │       │
└────┬────┘       │
     │            │
  ┌──┴──┐         │
  │     │         │
 Yes   No         │
  │     │         │
  ▼     ▼         ▼
┌──────────────────┐
│   Return Data    │
└──────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                         TYPE TRANSFORMATIONS                                │
└─────────────────────────────────────────────────────────────────────────────┘

Database (RPC) ──────────────▶ Service ──────────────▶ Store ──────────────▶ UI

UpcomingClassData             UpcomingClassData        UpcomingClassData    UpcomingClass
┌──────────────────┐          ┌──────────────────┐    ┌──────────────────┐ ┌────────────┐
│ enrollment_id    │          │ enrollment_id    │    │ enrollment_id    │ │ id         │
│ class_id         │          │ class_id         │    │ class_id         │ │ title      │
│ class_name       │──────────│ class_name       │────│ class_name       │─│ subject:{} │
│ subject          │          │ subject          │    │ subject          │ │ startTime  │
│ start_time       │          │ start_time       │    │ start_time       │ │ participants│
│ ...              │          │ ...              │    │ ...              │ └────────────┘
└──────────────────┘          └──────────────────┘    └──────────────────┘
                                                              │
                                                              │ mapUpcomingClassData()
                                                              │
                                                              ▼


┌─────────────────────────────────────────────────────────────────────────────┐
│                         ERROR HANDLING FLOW                                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────┐
│  API Call   │
└──────┬──────┘
       │
   ┌───┴───┐
   │ Error?│
   └───┬───┘
       │
 ┌─────┴─────┐
 │           │
Yes         No
 │           │
 ▼           ▼
┌─────────────────────┐    ┌─────────────────┐
│ Catch in Service    │    │ Return Success  │
│ • Log error         │    └─────────────────┘
│ • Return {          │
│     success: false, │
│     error: msg      │
│   }                 │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Store Updates       │
│ • Set error state   │
│ • Clear loading     │
│ • Remove inflight   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Component Renders   │
│ • Check error state │
│ • Show error UI     │
│ • Offer retry       │
└─────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                         FILES & RESPONSIBILITIES                            │
└─────────────────────────────────────────────────────────────────────────────┘

📁 types/branch-classes.types.ts
   └─► Defines: UpcomingClassData, UpcomingClassesResult, EnrollmentStatus

📁 services/branch-classes.service.ts
   └─► Implements: getUpcomingClasses(studentId) → calls RPC

📁 validations/branch-classes.validation.ts
   └─► Validates: upcomingClassesQuerySchema (student_id)

📁 stores/branch-classes.store.ts
   └─► Manages: Cache, Loading, Errors, Actions, Hooks

📁 utils/branch-classes.utils.ts
   └─► Transforms: RPC data → UI data, subject mapping, colors

📁 components/dashboard/learning-dashboard/index.tsx
   └─► Consumes: Store hooks, renders UI components

📁 components/dashboard/learning-dashboard/upcoming-class-card.tsx
   └─► Displays: Individual class cards with styling
```
